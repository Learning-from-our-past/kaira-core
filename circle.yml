machine:
  python:
    version: 3.5.3

dependencies:
  pre:
    - sudo apt-get install libfuzzy-dev ssdeep
    - pyenv local 2.7.12 3.5.3
    - invoke nlp-setup

test:
  override:
    - python -m pytest
